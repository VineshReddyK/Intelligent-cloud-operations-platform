server:
  port: 8086
  shutdown: graceful

spring:
  application:
    name: k8s-operator
  lifecycle:
    timeout-per-shutdown-phase: 30s

operator:
  reconcile-interval-ms: ${RECONCILE_INTERVAL_MS:30000}

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}/v1/traces

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.icop: ${LOG_LEVEL:INFO}
    io.fabric8: INFO
